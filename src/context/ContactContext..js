import { createContext } from "react";


// createContext is going to create a special object that is going to hold all the information from your context
export const ContactsContext = createContext(null)